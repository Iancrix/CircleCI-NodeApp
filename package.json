{
	"name": "petworld",
	"version": "1.0.0",
	"description": "PetWorld Project - MERN Stack Application",
	"main": "server.js",
	"scripts": {
		"start": "pm2 -f start server.js",
		"dev": "nodemon server",
		"test": "npm install --prefix frontend && npm run build --prefix frontend && npm test --prefix frontend",
		"deploy": "npm install --prefix frontend && npm run build --prefix frontend  && npm install"
	},
	"author": "Ian Chona",
	"license": "ISC",
	"dependencies": {
		"@azure/storage-blob": "^12.2.1",
		"azure-storage": "^2.10.3",
		"cookie-parser": "^1.4.5",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"into-stream": "^6.0.0",
		"mongoose": "^5.9.18",
		"morgan": "^1.10.0",
		"multer": "^1.4.2"
	},
	"devDependencies": {
		"nodemon": "^2.0.4"
	},
	"repository": {
		"type": "git",
		"url": "git://github.com/Iancrix/Petworld.git"
	}
}